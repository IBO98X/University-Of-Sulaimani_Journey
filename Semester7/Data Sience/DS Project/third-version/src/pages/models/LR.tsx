import ModelPage from "./ModelPage";

export default function LR() {
  return (
    <ModelPage
      modelId="lr"
      title="Linear/Probabilistic Model — Logistic Regression"
      description="A baseline linear probabilistic classifier that estimates class probabilities using a logistic function. Simple, fast, and interpretable benchmark for comparison."
    />
  );
}
